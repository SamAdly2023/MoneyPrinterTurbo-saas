# Keep legacy pip install support; primary deps moved to pyproject.toml / uv.lock.
moviepy==2.2.1
streamlit==1.58.0
edge_tts==7.2.7
fastapi==0.136.3
uvicorn==0.32.1
openai==2.24.0
faster-whisper==1.1.0
loguru==0.7.3
google.generativeai==0.8.6
dashscope==1.20.14
azure-cognitiveservices-speech==1.41.1
redis==5.2.0
python-multipart==0.0.27
pyyaml==6.0.3
requests==2.33.1
socksio==1.0.0
pydub==0.25.1
litellm==1.86.2
itsdangerous==2.2.0
firebase-admin==6.9.0
# MySQL backend for cPanel/shared-hosting deployments (MPT_DB=mysql) - see
# app/services/db_mysql.py. Pure-Python, no compiled C extension, so it
# installs cleanly on shared hosting without a compiler.
PyMySQL==1.2.0
# PostgreSQL backend (MPT_DB=postgres) - see app/services/db_postgres.py.
# The -binary build ships precompiled wheels, same reasoning as PyMySQL
# above: no C compiler needed on shared hosting.
psycopg2-binary==2.9.12
# Replay Channels: importing a video from a link (app/services/replay.py's
# _import_via_ytdlp) when it's a video-site page rather than a raw file -
# pure Python, no compiled extension, same shared-hosting-friendly profile
# as the two packages above.
yt-dlp==2026.8.19

# Optional unofficial g4f provider is intentionally not installed by default.
# Use `uv sync --extra g4f` only when you explicitly accept that provider risk.
